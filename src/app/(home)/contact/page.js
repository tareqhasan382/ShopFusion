import ContactForm from "@/components/ContactForm";

export const metadata = {
  title: "Contact Us",
  description: "Get in touch with the ShopFusion support team.",
};

const page = () => <ContactForm />;

export default page;
