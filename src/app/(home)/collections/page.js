import CollectionsListing from "@/components/UI/CollectionsListing";

export const metadata = {
  title: "Collections",
  description: "Browse every ShopFusion collection and shop by category.",
};

const CollectionsPage = () => <CollectionsListing />;

export default CollectionsPage;
